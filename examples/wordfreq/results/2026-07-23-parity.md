# Parity result — wordfreq (Python → Rust)

**Date:** 2026-07-23
**Verdict:** ✅ PASS — the Rust port produces byte-identical output to the Python
reference across every case.

## Environment

| | Version |
| --- | --- |
| Python (reference) | 3.13.4 |
| Rust (port) | rustc 1.96.1 (2026-06-26), release build |
| Platform | Windows 11 (x86-64) |

Both implementations are standard-library only — no third-party dependencies.

## What was tested

`parity.sh` runs both binaries on `fixtures/sample.txt` and compares output
byte-for-byte (after stripping the OS `\r` newline artifact). The Python output
is the oracle; the port must match it exactly.

| # | Case | Result |
| --- | --- | --- |
| 1 | default, stdin (top 10) | PASS |
| 2 | `-n 3`, stdin | PASS |
| 3 | `-n 100` (past vocabulary), stdin | PASS |
| 4 | `-n 0` (empty output), stdin | PASS |
| 5 | `-n 5`, file argument | PASS |

```
Building the Rust port...
PASS  default (top 10)
PASS  top 3
PASS  top 100 (all)
PASS  top 0 (none)
PASS  file arg (top 5)

All parity cases passed — the port matches the reference.
```

Sample output (`-n 5 fixtures/sample.txt`), identical from both:

```
7	the
4	a
4	and
4	dog
4	fox
```

## Size

| Implementation | Lines |
| --- | --- |
| `python/wordfreq.py` | 59 |
| `rust/src/main.rs` | 89 |

The port is larger, as expected for an explicitly-typed systems language with
manual argument parsing — the value here is *behavioral equivalence*, not fewer
lines.

## Reproduce

```bash
cd examples/wordfreq && bash parity.sh
```

Toolchain versions will differ on your machine; the parity comparison itself is
platform-independent (newline differences are normalized in the harness).
